name: repositories
description: A new Flutter project.


publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  injectable: ^2.1.0
  firebase_auth: ^4.2.1
  models:
    path: ../models
  servises:
    path: ../servises

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^2.0.0